import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    //alignItems: 'center',
    //justifyContent: 'center',
  },

  textClass: {
    color: '#00f',
    fontSize: 15,
  },
  textTitulo: {
    color: '#f00',
    fontSize: 30,
  },
});
